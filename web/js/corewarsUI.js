class CoreWarsUI {
    constructor(canvas) {
        this.canvas = canvas

        // get the context
        this.ctx = this.canvas.getContext("2d")
    }

    reset() {
        // init or reset the UI
        // Fill entire canvas black
    }
}